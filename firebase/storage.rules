rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow reads/writes to cast media during development/testing.
    match /casts/{castId}/media/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
